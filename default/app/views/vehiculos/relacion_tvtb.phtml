<div class="wrapper">
    <div class="header">

    </div>
    <div class="main main-raised">
        <div class="container" style="margin-top: 40px">                           
            <?php View::content() ?>
            <div class="container">
                <div class="title">
                    <h3>Relacionar con Trabajo</h3>
                </div>
                <h5>Tipo de Vehiculo: <?php echo $tvehiculos->nombre ?></h5>
            </div>
            <div class="container">
                <div class="table-responsive">
                    <div class="col-md-8">                                                      
                        <table id="relacion_user" class="table">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Nombre</th>
                                    <th>Relación</th>                                                
                                </tr>
                            </thead>
                            <tbody>                                            
                                <?php
                                foreach ($page as $listado):
                                    $flag = 0;
                                    ?>
                                    <tr>
                                        <td><?php echo $listado->id ?></td>
                                        <td><?php echo $listado->nombre ?></td>
                                        <td>
                                            <div class="togglebutton">
                                                <label>
                                                    <?php if ($rela == FALSE) { ?>
                                                        <input type="checkbox" id="<?php echo $listado->id ?>" value="<?php echo $tvehiculos->id ?>" onchange="tvtb(this.checked, this.id, this.value)">                                                                    
                                                        <?php
                                                    } else {
                                                        foreach ($rela as $tvtb):
                                                            if ($listado->id == $tvtb->trabajos_id) {
                                                                $flag = $tvtb->id;
                                                            }
                                                        endforeach;
                                                        echo $flag != 0 ? '<input type="checkbox" id="' . $flag . '" value="0" checked="" onchange="tvtb(this.checked, this.id, this.value)">' : '<input type="checkbox" id="' . $listado->id . '" value="' . $tvehiculos->id . '" onchange="tvtb(this.checked, this.id, this.value)">';
                                                    }
                                                    ?>
                                                </label>
                                            </div>
                                        </td>
                                    </tr> 
                                <?php endforeach; ?>
                            </tbody>
                        </table>                                
                    </div>
                </div>
            </div>                
        </div>
    </div>    
</div>